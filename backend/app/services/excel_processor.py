class ExcelProcessor:
    """
    Helper class for custom Excel sheet processing.
    Currently, pandas functions are handled directly in the Celery pipeline tasks 
    for performance and atomicity, but this file is kept for future scaling options.
    """
    pass
